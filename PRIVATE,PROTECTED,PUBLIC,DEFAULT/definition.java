package PRIVATE,PROTECTED,PUBLIC,DEFAULT;

public class definition {
  /*ACCESS SPECIFIERS:
    Default:if nothing is written bfore functions like samples, then that is default. available fully in that package.
    Private:if a function is written in a class, then that fnctn can only be used by that class even if its object is created  too.
    protected:will be available only in that class, and its inherited one.
    public:will be available outside package too.
    
}
