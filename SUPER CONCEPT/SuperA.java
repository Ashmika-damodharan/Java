//In method overriding we learned that if a class and its inherited one has same method and if we call the method, the one in the child class is executed.now if we want to execute the method in parent class, super in used.

public class SuperA 
{
    int a;
    void display()
    {
        System.out.println("Its A");
    }
    
}
